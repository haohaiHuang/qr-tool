# PLAN — QR 码矢量增强工具（方案 B 路线）

> 阶段 0-2 已完成（骨架/基础设施/重编码引擎 A）。本计划以**方案 B（结构重绘）**为主推进。

## 阶段 0：Harness 与骨架 ✅
AGENTS/SPEC/PLAN/TASKS/feature_list/progress/init.sh；npm test 链（33 测试）

## 阶段 1：核心基础设施 ✅
像素工具（T1.1）、Finder 检测（T1.2）、圆环检测（T1.3）、识别路由（T1.4）

## 阶段 2：重编码引擎 A（兜底）✅
解码（jsQR→zxing→抹白）、重生、Otsu 配色、闭环自检、UI 基础版

## 阶段 3：**方案 B 结构重绘引擎（本次核心）**

| 任务 | 内容 | Test Case |
|---|---|---|
| T3.1 网格定位 | finder 仿射映射 + 对齐图案校正 → 网格映射 | TC-G1~G4 |
| T3.2 模块采样 | 逐模块中心多数投票 → 原模块矩阵 | TC-S1~S3 |
| T3.3 高清重绘 | 原矩阵矢量重绘 + Otsu 配色 + **logo 中心原样叠加** | TC-R1~R3 |
| T3.4 管道集成 | enhance2：B 优先 → 失败回退 A；含自检 | TC-E1~E4 |
| T3.5 可行性闸门 | 真实图（IMG6150）采样重绘可扫码才放行 | TC-S3, TC-E1 |

**闸门（Gate）**：T3.5 不通过 → 调整定位精度策略（透视校正/投影细化），不进入 UI 阶段

## 阶段 4：UI 更新（B 结果）
- 对比预览（原图 vs 结构重绘）
- logo 保留提示、引擎使用标记（B/A）
- 微信码引导（API 重生成）

## 阶段 5：微信码视觉重绘
- 复用网格定位思路（同心圆 + 辐射坑位）
- 结构重建（网格固定性验证 + 自洽测试）通过才开放

## 阶段 6：打磨（P2）
批量、彩色/渐变、透视/旋转、处理报告完善

## 依赖图

```
阶段0/1/2（完成）→ 阶段3（B引擎，Gate 后放行）
                       ↓
                  阶段4（UI）
                  阶段5（微信码，可并行）
                       ↓
                  阶段6（打磨）
```

## 开发节奏（TDD）

1. **T3.1 网格定位**（最难，先攻）→ T3.2 采样 → T3.3 重绘 → T3.4 管道
2. 每任务 Red→Green→Refactor，`npm test` 必须绿
3. **Gate 验收**：每阶段完成跑 `./init.sh` + 真实图端到端
4. 测试图素材：ui/test-qr.png（微信码）、/tmp/img6150.rgba（真实图调试）
