# PLAN — QR 码矢量增强工具

> 把 SPEC 拆成阶段。每阶段可独立交付、有验证标准。TDD 在 TASKS.md 的任务层执行。

## 阶段 0：Harness 与骨架（当前）
- [x] AGENTS.md / SPEC.md / PLAN.md / TASKS.md / feature_list.json / progress.md / init.sh
- [x] 项目结构（src/、test/、ui/）
- [ ] package.json + npm test 骨架（一个占位测试跑通）
- 验证：`./init.sh` 通过

## 阶段 1：核心基础设施（P0 依赖）
- 像素数组工具（加载/灰度/二值化/裁剪/缩放）
- 类型识别路由（定位符检测 / 圆环检测）
- 验证：每个模块 TDD（node:test）

## 阶段 2：标准码解码重生（F3，P0 第一优先级）
- 解码 → 重生 → 风格保留 → 闭环自检
- 验证：SPEC F3 验收（再解码 = 原数据）

## 阶段 3：微信码视觉重绘（F4，P0）
- 环检测 → 辐射解析 → 坑位检测 → 矢量重绘
- 验证：SPEC F4 验收

## 阶段 4：UI HTML（P0 收口）
- 单 HTML：上传 → 识别 → 处理预览（对比滑块）→ 下载 PNG/SVG
- 集成所有引擎

## 阶段 5：Logo 码（F5，P1）
- 依赖阶段 2；加 Logo 检测/抠取/叠加/纠错余量检查

## 阶段 6：微信码结构重建（F6，P2，前置验证）
- 网格固定性验证 + 自洽测试通过后才开放
- 依赖阶段 3

## 阶段 7：批量与打磨（P2）
- 批量队列 + ZIP、彩色码、处理报告完善

## 依赖图

```
阶段0 → 阶段1 → 阶段2 → 阶段4（MVP 收口）
                  ↘ 阶段3 ↗        ↘ 阶段5
                        阶段3 → 阶段6（验证后）
阶段4 → 阶段7
```

## MVP 判定

阶段 0–4 完成后即为可交付 MVP（标准码 + 微信视觉重绘 + UI）。
