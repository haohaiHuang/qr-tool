# SPEC — QR 码矢量增强工具（v3，方案 B：结构重绘）

> 核心变更：主方案从"解码→重编码"（A）改为"**保留原结构高清重绘**"（B）。
> 原码模块排列、配色、logo 全部保留，数据天然一致。A 降级为兜底。

## 1. 产品定位

纯前端（单 HTML）工具：低分辨率 QR 码增强为高清 PNG/SVG，用于印刷。
**核心原则：增强而非替换**——重绘结果必须**视觉接近原码**（模块排列 1:1、配色保留、logo 保留），且扫码内容与原码**逐字符一致**。

## 2. 双引擎架构

| 引擎 | 原理 | 视觉 | 数据 | 用途 |
|---|---|---|---|---|
| **B 结构重绘（主）** | 定位原模块网格 → 采样 → 高清重绘 | ✅ 1:1 接近原码（含 logo/配色） | 天然一致（未重编码） | 默认路径 |
| A 重编码（兜底） | 解码 → qrcode-generator 重生 | 模块排列不同 | 数据等价（自检保证） | B 定位失败时回退 |

**回退规则**：B 失败（定位不精确/采样噪声大）→ 尝试 A；A 也失败 → 明确报错。

## 3. 功能规格

### F1 输入层
- 拖拽/粘贴/文件选择；手动框选（可选）
- TC: TC-E4

### F2 类型识别路由
- 三 Finder → 标准 QR；同心圆 → 微信码；否则 unknown
- 验收：3 类样本识别正确率 ≥ 90%
- TC: TC-F2

### F3 模块网格精确定位（方案 B 核心）
- 输入：QR 图 → 输出：网格映射（每模块边界/中心）
- 用 3 Finder 建立仿射映射 + 对齐图案校正（version≥2）
- 验收：合成码网格误差 ≤ 0.5 模块；模糊/JPEG 码误差 ≤ 1 模块
- TC: TC-G1~G4

### F4 逐模块采样（方案 B 核心）
- 每模块中心区域多数投票 → 原模块矩阵
- 验收：合成码一致率 100%；模糊码 ≥ 95%
- TC: TC-S1~S3

### F5 高清重绘 + 风格保留（方案 B 核心）
- 按原矩阵矢量重绘（任意分辨率）
- **配色保留**（Otsu 前/背景色）
- **logo 保留**（中心 logo 区域原样叠加——区别于 A 的丢失）
- 验收：重绘后扫码 = 原内容；配色/logo 视觉保留
- TC: TC-R1~R3

### F6 回退引擎（A 重编码）
- B 失败时：解码 → 重生（jsQR→zxing→抹白重解）→ 自检
- 验收：扫码内容与原码一致（逐字符）
- TC: TC-E2

### F7 输出与预览
- 原图 vs 重绘对比滑块；处理报告；下载 PNG（4000px）/SVG
- TC: TC-E1

### F8 数据完整性（埋点/参数保留）— 基础约束
- 输出码扫码内容必须与原码**逐字符一致**（URL 参数/埋点/tracking 全保留，不重写不截断）
- 双引擎共同保证：B 天然一致；A 数据等价 + 自检比对
- TC: TC-D1（带埋点 URL 的码，增强后逐字符一致）

## 4. 测试用例（Test Case）

### 定位模块（grid.js）
| TC | 输入 | 期望 |
|---|---|---|
| TC-G1 | 合成码（纯黑白，quiet=4） | 网格误差 ≤ 0.5 模块 |
| TC-G2 | 合成码 quiet=8/12 | 网格误差 ≤ 0.5 模块 |
| TC-G3 | 模糊/JPEG 码（Gaussian+压缩） | 网格误差 ≤ 1 模块 |
| TC-G4 | 纯白图 | 返回 null（无码） |

### 采样模块（sampler.js）
| TC | 输入 | 期望 |
|---|---|---|
| TC-S1 | 合成码 | 采样矩阵 = 原矩阵（100%） |
| TC-S2 | 模糊码 | 一致率 ≥ 95% |
| TC-S3 | 真实图（IMG6150 微信码） | 采样矩阵重绘后可扫码 = 原内容 |

### 重绘模块（render.js）
| TC | 输入 | 期望 |
|---|---|---|
| TC-R1 | 采样矩阵 → 重绘 8px 模块 | 重绘扫码 = 原内容 |
| TC-R2 | 彩色码 | 重绘前/背景色 ≈ 原色（±30） |
| TC-R3 | 带 logo 码 | 重绘中心含 logo（非全白/全黑） |

### 管道（enhance2.js）
| TC | 输入 | 期望 |
|---|---|---|
| TC-E1 | 标准码 | B 成功：扫码一致 + 视觉接近（矩阵一致率 ≥ 95%） |
| TC-E2 | 带 logo 码 | B 成功：扫码一致 + logo 保留 |
| TC-E3 | B 失败场景（构造噪声） | 回退 A：扫码一致 |
| TC-E4 | 白图/无码 | 明确失败原因 |

### 数据完整性
| TC | 输入 | 期望 |
|---|---|---|
| TC-D1 | 带埋点 URL（query 参数）的码 | 增强后扫码逐字符一致 |

## 5. 诚实分级（更新）

| 引擎 | 承诺 |
|---|---|
| B 结构重绘 | "视觉接近原码（含 logo/配色）+ 扫码内容一致" |
| A 重编码（回退） | "扫码内容一致（数据等价）；视觉为重新编码样式" |
| 微信码 | 视觉重绘（结构重建待验证）；扫码成功率不保证 |
