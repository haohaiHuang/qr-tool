# SPEC — QR 码矢量增强工具

> 来源：`~/Desktop/Pi/QR-Tool-Enhancer-SPEC-v2.md`（设计提案）。本文件是可执行规格（验收标准）。
> 状态：v0.1 DRAFT · 2026-08-17

## 1. 产品定位

纯前端（单 HTML，零服务器）工具：把低分辨率 QR 码 / 微信码增强为高清 PNG/SVG，用于印刷物料。核心原则：**可验证性优先、诚实分级、增强而非替换**。

## 2. 功能规格（P0 = MVP 必须）

### F1 输入层
- 拖拽 / 粘贴 / 文件选择导入图片
- 手动框选码区域（可选，降低定位失败）

### F2 类型识别路由
- 检测三回字定位符 → 标准 QR
- 检测同心圆环 → 微信码
- 无法识别 → 提示 + 降级选项
- 验收：对 3 类样本（标准码/Logo码/微信码）识别正确率 ≥ 90%

### F3 标准 QR 码 — 解码重生（P0，第一优先级）
- 解码（jsQR）→ 高分辨率重生（PNG 4000px / SVG）
- **风格保留**：检测原码配色/模块形状，重生时还原
- **闭环自检**：重生结果再解码，数据一致才输出
- 验收（核心）：给定标准码图片，输出码再次解码 = 原数据；任意分辨率可扫

### F4 微信码 — 视觉重绘（P0）
- 环检测 → 辐射解析 → 坑位定位 → 高分辨率矢量重绘
- 输出标注："视觉增强，扫码成功率不保证"
- 流程优先引导用户"微信 API 重新生成"
- 验收：视觉上清晰（对比度/边缘），结构元素数量与输入一致

### F5 Logo 码（P1，**Logo 保留未实现，待 T5.x**）
- ⚠️ 当前只实现"解码恢复"（jsQR→zxing→抹白重解），**重生输出为干净码，logo 已丢失**
- 待实现：解码纠错 → 重生 → **自动抠原图 Logo → 原位叠加**（T5.1/T5.3）
- 纠错余量检查（Logo 覆盖面积 vs 纠错等级上限，T5.2）
- 验收：输出数据与原码一致，**Logo 视觉保留**（增强而非替换）

### F8 数据完整性（埋点保留）— 基础约束
- **原则**：重生码必须完整保留原始解码数据字符串，**不做任何重写/截断/规范化**（URL 参数、埋点 tracking、自定义字段全部原样）
- 现状：已天然保证（解码→重生是数据级无损 + 闭环自检比对完整字符串）
- 验收：带 query 参数/埋点的码，增强后解码内容逐字符一致

### F6 微信码 — 结构重建（P2，前置验证）
- 仅当"网格固定性验证 + 自洽测试"通过后开放
- 自洽测试：高清原图降采样 → 重描 → 对比坑位矩阵
- 验收：内置样本矩阵一致性 ≥ 95% 才开放此档

### F7 输出与预览
- 原图 vs 结果对比滑块
- 处理报告（识别结果/检测数/验证结果/成功率预估）
- 下载 PNG / SVG；批量（P2）

## 3. 技术约束

- 核心算法纯 JS + 像素数组（node 可单测，不依赖 DOM/Canvas）
- UI 薄层：文件 → 像素数组 → 核心 → 渲染
- 测试：node:test 零依赖；`npm test` 为验证命令
- MVP 不引 OpenCV.js / 不做 AI 超分

## 4. 边界与诚实分级

| 档位 | 承诺 |
|---|---|
| 标准码解码重生 | "扫描结果与原码一致"（闭环验证） |
| Logo 码 | "数据一致，Logo 视觉近似" |
| 微信码视觉重绘 | "视觉清晰，扫码成功率不保证" |
| 微信码结构重建 | 验证通过后承诺"可扫（成功率 X%）" |

## 5. 待定（需用户确认）

- 微信码 vs 标准码真实占比（影响 F6 优先级）
- 风格保留程度（仅配色 / 形状渐变全还原）
- 真实小程序码样本（F6 前置验证用）
